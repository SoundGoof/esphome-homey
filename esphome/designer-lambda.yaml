|-
  // Blank the panel when Homey has switched the display off, before anything is
  // drawn. ePaper holds its last image with no power, so sleeping without
  // clearing would leave stale data on screen.
  if (!id(display_on)) {
    it.fill(Color(255, 255, 255));
    return;
  }
  const auto COLOR_WHITE = Color(0, 0, 0);
  const auto COLOR_BLACK = Color(255, 255, 255);
  auto color_off = COLOR_WHITE;
  auto color_on = COLOR_BLACK;

  auto print_wrapped_text = [&](int x, int y, int max_w, int line_h, esphome::font::Font *font, Color color, TextAlign align, const char* text) {
    if (!text || max_w <= 0) return;
    int cy = y;
    std::string line, word;
    const char* p = text;
    auto flush_word = [&]() {
      if (word.empty()) return;
      int ww, wh, wbl, wx;
      font->measure(word.c_str(), &ww, &wx, &wbl, &wh);
      int lw = 0, lx;
      if (!line.empty()) {
        font->measure(line.c_str(), &lw, &lx, &wbl, &wh);
        int sw, sx, sbl, sh;
        font->measure(" ", &sw, &sx, &sbl, &sh);
        lw += sw;
      }
      if (lw + ww > max_w && !line.empty()) {
        it.print(x, cy, font, color, align, line.c_str());
        cy += line_h;
        line = word;
      } else {
        if (!line.empty()) line += " ";
        line += word;
      }
      word.clear();
    };
    while (*p) {
      if (*p == ' ' || *p == '\n' || *p == '\r' || *p == '\t') flush_word();
      else word += *p;
      p++;
    }
    flush_word();
    if (!line.empty()) it.print(x, cy, font, color, align, line.c_str());
  };
  int currentPage = id(display_page);
  if (currentPage == 0) {
    it.fill(COLOR_BLACK);
    color_off = COLOR_BLACK;
    color_on = COLOR_WHITE;
            {
              auto now = id(ha_time).now();
              it.strftime(510, 40, id(font_roboto_700_240), color_on, TextAlign::TOP_CENTER, "%H:%M", now);
              it.strftime(510, 320, id(font_roboto_400_48), color_on, TextAlign::TOP_CENTER, "%a, %b %d", now);
            }
            {
              char wrap_buf[512];
            sprintf(wrap_buf, "%s", id(homey_t_headline).state.c_str());
              print_wrapped_text(60, 400, 1750, 123, id(font_roboto_400_110), color_on, TextAlign::TOP_LEFT, wrap_buf);
            }
            {
              char wrap_buf[512];
            sprintf(wrap_buf, "%s", id(homey_t_slot1_caption).state.c_str());
              print_wrapped_text(60, 580, 560, 49, id(font_roboto_400_44), color_on, TextAlign::TOP_LEFT, wrap_buf);
            }
            {
              char wrap_buf[512];
              if (std::isnan(id(homey_n_slot1_value).state))
                sprintf(wrap_buf, "\u2014");
              else
                sprintf(wrap_buf, "%.1f %s", id(homey_n_slot1_value).state, id(homey_t_slot1_unit).state.empty() ? "${slot1_unit}" : id(homey_t_slot1_unit).state.c_str());
              print_wrapped_text(60, 642, 560, 134, id(font_roboto_400_120), color_on, TextAlign::TOP_LEFT, wrap_buf);
            }
            {
              char wrap_buf[512];
            sprintf(wrap_buf, "%s", id(homey_t_slot2_caption).state.c_str());
              print_wrapped_text(656, 580, 560, 49, id(font_roboto_400_44), color_on, TextAlign::TOP_LEFT, wrap_buf);
            }
            {
              char wrap_buf[512];
              if (std::isnan(id(homey_n_slot2_value).state))
                sprintf(wrap_buf, "\u2014");
              else
                sprintf(wrap_buf, "%.1f %s", id(homey_n_slot2_value).state, id(homey_t_slot2_unit).state.empty() ? "${slot2_unit}" : id(homey_t_slot2_unit).state.c_str());
              print_wrapped_text(656, 642, 560, 134, id(font_roboto_400_120), color_on, TextAlign::TOP_LEFT, wrap_buf);
            }
            {
              char wrap_buf[512];
            sprintf(wrap_buf, "%s", id(homey_t_slot3_caption).state.c_str());
              print_wrapped_text(1252, 580, 560, 49, id(font_roboto_400_44), color_on, TextAlign::TOP_LEFT, wrap_buf);
            }
            {
              char wrap_buf[512];
              if (std::isnan(id(homey_n_slot3_value).state))
                sprintf(wrap_buf, "\u2014");
              else
                sprintf(wrap_buf, "%.1f %s", id(homey_n_slot3_value).state, id(homey_t_slot3_unit).state.empty() ? "${slot3_unit}" : id(homey_t_slot3_unit).state.c_str());
              print_wrapped_text(1252, 642, 560, 134, id(font_roboto_400_120), color_on, TextAlign::TOP_LEFT, wrap_buf);
            }
            {
              char wrap_buf[512];
            sprintf(wrap_buf, "%s", id(homey_t_slot4_caption).state.c_str());
              print_wrapped_text(60, 830, 560, 49, id(font_roboto_400_44), color_on, TextAlign::TOP_LEFT, wrap_buf);
            }
            {
              char wrap_buf[512];
              if (std::isnan(id(homey_n_slot4_value).state))
                sprintf(wrap_buf, "\u2014");
              else
                sprintf(wrap_buf, "%.1f %s", id(homey_n_slot4_value).state, id(homey_t_slot4_unit).state.empty() ? "${slot4_unit}" : id(homey_t_slot4_unit).state.c_str());
              print_wrapped_text(60, 892, 560, 134, id(font_roboto_400_120), color_on, TextAlign::TOP_LEFT, wrap_buf);
            }
            {
              char wrap_buf[512];
            sprintf(wrap_buf, "%s", id(homey_t_slot5_caption).state.c_str());
              print_wrapped_text(656, 830, 560, 49, id(font_roboto_400_44), color_on, TextAlign::TOP_LEFT, wrap_buf);
            }
            {
              char wrap_buf[512];
              if (std::isnan(id(homey_n_slot5_value).state))
                sprintf(wrap_buf, "\u2014");
              else
                sprintf(wrap_buf, "%.1f %s", id(homey_n_slot5_value).state, id(homey_t_slot5_unit).state.empty() ? "${slot5_unit}" : id(homey_t_slot5_unit).state.c_str());
              print_wrapped_text(656, 892, 560, 134, id(font_roboto_400_120), color_on, TextAlign::TOP_LEFT, wrap_buf);
            }
            {
              char wrap_buf[512];
            sprintf(wrap_buf, "%s", id(homey_t_slot6_caption).state.c_str());
              print_wrapped_text(1252, 830, 560, 49, id(font_roboto_400_44), color_on, TextAlign::TOP_LEFT, wrap_buf);
            }
            {
              char wrap_buf[512];
              if (std::isnan(id(homey_n_slot6_value).state))
                sprintf(wrap_buf, "\u2014");
              else
                sprintf(wrap_buf, "%.1f %s", id(homey_n_slot6_value).state, id(homey_t_slot6_unit).state.empty() ? "${slot6_unit}" : id(homey_t_slot6_unit).state.c_str());
              print_wrapped_text(1252, 892, 560, 134, id(font_roboto_400_120), color_on, TextAlign::TOP_LEFT, wrap_buf);
            }
            it.printf(60, 1060, id(font_roboto_400_44), color_on, TextAlign::TOP_LEFT, "Panel temp");
            {
              char wrap_buf[512];
              if (std::isnan(id(sht_temperature).state))
                sprintf(wrap_buf, "\u2014");
              else
                sprintf(wrap_buf, "%.1f °C", id(sht_temperature).state);
              print_wrapped_text(60, 1122, 420, 110, id(font_roboto_400_96), color_on, TextAlign::TOP_LEFT, wrap_buf);
            }
            it.printf(516, 1060, id(font_roboto_400_44), color_on, TextAlign::TOP_LEFT, "Panel humidity");
            {
              char wrap_buf[512];
              if (std::isnan(id(sht_humidity).state))
                sprintf(wrap_buf, "\u2014");
              else
                sprintf(wrap_buf, "%.0f %%", id(sht_humidity).state);
              print_wrapped_text(516, 1122, 420, 110, id(font_roboto_400_96), color_on, TextAlign::TOP_LEFT, wrap_buf);
            }
            it.printf(972, 1060, id(font_roboto_400_44), color_on, TextAlign::TOP_LEFT, "Panel voltage");
            {
              char wrap_buf[512];
              if (std::isnan(id(battery_voltage).state))
                sprintf(wrap_buf, "\u2014");
              else
                sprintf(wrap_buf, "%.2f V", id(battery_voltage).state);
              print_wrapped_text(972, 1122, 420, 110, id(font_roboto_400_96), color_on, TextAlign::TOP_LEFT, wrap_buf);
            }
            it.printf(1428, 1060, id(font_roboto_400_44), color_on, TextAlign::TOP_LEFT, "Panel battery");
            {
              char wrap_buf[512];
              if (std::isnan(id(battery_level).state))
                sprintf(wrap_buf, "\u2014");
              else
                sprintf(wrap_buf, "%.0f %%", id(battery_level).state);
              print_wrapped_text(1428, 1122, 420, 110, id(font_roboto_400_96), color_on, TextAlign::TOP_LEFT, wrap_buf);
            }
            {
              // Touch button. The hit area in the YAML matches this rectangle;
              // move one and you must move the other. Label and state come from
              // Homey, so what the button controls is a Flow decision.
              it.rectangle(60, 1258, 440, 120, color_on);
              it.rectangle(62, 1260, 436, 116, color_on);
              std::string label = id(homey_t_button1_label).state;
              if (label.empty()) label = "Button";
              std::string state = id(homey_t_button1_state).state;
              it.printf(80, 1288, id(font_roboto_400_44), color_on, TextAlign::TOP_LEFT, "%s", label.c_str());
              if (!state.empty())
                it.printf(480, 1288, id(font_roboto_400_44), color_on, TextAlign::TOP_RIGHT, "%s", state.c_str());
            }
            {
              std::string raw_state = id(homey_t_weather).state;
              std::string weather_state = "";
              for (auto &c : raw_state) weather_state += tolower(c);
              const char* icon = "\U000F0625";
              if (false) {}
              else if (weather_state == "clear-night") icon = "\U000F0594";
              else if (weather_state == "cloudy") icon = "\U000F0590";
              else if (weather_state == "exceptional") icon = "\U000F0026";
              else if (weather_state == "fog") icon = "\U000F0591";
              else if (weather_state == "hail") icon = "\U000F0592";
              else if (weather_state == "lightning") icon = "\U000F0593";
              else if (weather_state == "lightning-rainy") icon = "\U000F067E";
              else if (weather_state == "partlycloudy-night") icon = "\U000F0F31";
              else if (weather_state == "partlycloudy") icon = "\U000F0595";
              else if (weather_state == "pouring") icon = "\U000F0596";
              else if (weather_state == "rainy") icon = "\U000F0597";
              else if (weather_state == "snowy") icon = "\U000F0598";
              else if (weather_state == "snowy-rainy") icon = "\U000F067F";
              else if (weather_state == "sunny") icon = "\U000F0599";
              else if (weather_state == "windy") icon = "\U000F059D";
              else if (weather_state == "windy-variant") icon = "\U000F059E";
              it.printf(1600, 210, id(font_material_design_icons_400_260), color_on, TextAlign::CENTER, "%s", icon);
            }
  }
