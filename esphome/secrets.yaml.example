# Copy to secrets.yaml and fill in. secrets.yaml is gitignored.
wifi_ssid: "your-ssid"
wifi_password: "your-wifi-password"

# Generate with: esphome secrets generate  (or any 32-byte base64 value)
api_encryption_key: "REPLACE_WITH_BASE64_32_BYTES"
ota_password: "REPLACE_WITH_A_PASSWORD"
